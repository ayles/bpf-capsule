# PureDOOM patches

These patches modify the pinned PureDOOM GPL-2.0 source used only by the Doom
example. Context lines shown in the patch files come from that upstream source;
the added lines are part of this GPL-2.0-only example. The first patch adds the
required modification notice, and the other two contain the documented
rendering fixes.
