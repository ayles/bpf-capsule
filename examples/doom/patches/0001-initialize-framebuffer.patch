diff --git a/PureDOOM.h b/PureDOOM.h
--- a/PureDOOM.h
+++ b/PureDOOM.h
@@ -16484,6 +16484,8 @@ void I_SetPalette(byte* palette)
 void I_InitGraphics(void)
 {
     screens[0] = (unsigned char*)doom_malloc(SCREENWIDTH * SCREENHEIGHT);
+    // malloc storage is not zero-initialized; zero it for determinism.
+    doom_memset(screens[0], 0, SCREENWIDTH * SCREENHEIGHT);
 }
 char* sprnames[NUMSPRITES + 1] = {
